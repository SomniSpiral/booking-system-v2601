version: '3.8'

services:
  app:
    build: .
    container_name: laravel_app
    ports:
      - "8000:80"
    environment:
      APP_ENV: production
      APP_KEY: base64:wVK+5eQXT7diiqrOeilrWT9nwcsZb4VgI8dOYoEbfWA=
      APP_DEBUG: "false"
      APP_URL: https://booking-system-v2601.onrender.com
      DB_CONNECTION: mysql
      DB_HOST: booking-db-booking-system-v2601.a.aivencloud.com
      DB_PORT: 10894
      DB_DATABASE: defaultdb
      DB_USERNAME:  ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_SSLMODE: verify_ca
      DB_SSL_CERT_PATH: /opt/render/project/src/ssl/ca.pem
      MAIL_MAILER: smtp
      MAIL_HOST: smtp.gmail.com
      MAIL_PORT: 587
      MAIL_USERNAME: ${MAIL_USERNAME}
      MAIL_PASSWORD: ${MAIL_PASSWORD}
      MAIL_ENCRYPTION: tls
      MAIL_FROM_ADDRESS: ${MAIL_USERNAME}
      MAIL_FROM_NAME: Booking Services
    volumes:
      - .:/var/www/html